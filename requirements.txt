# Day 1 scope: data generator, audit trail, decision layer, tests.
# (FastAPI / LiveKit / telephony come in later passes.)
pydantic>=2.6
psycopg[binary]>=3.1
python-dotenv>=1.0
Faker>=24.0
tzdata>=2024.1          # zoneinfo needs this on Windows
pytest>=8.0
