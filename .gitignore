# secrets / local config
.env
*.pem
global-bundle.pem

# local-only project docs — kept on disk, never pushed (Sept 4: purged
# from git history at the owner's request; do not re-track these)
CLAUDE.md
PRD.md

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# generated artifacts
data/fixtures/events.json
exports/
